#! /Applications/Racket v6.0.1/bin/racket
#lang racket/base

;; This sucks to write out so I'm skipping it for now.
;;
;; An Interval has three mutually exclusive states:
;;   all positive interval
;;   all negative interval
;;   spans 0
;;
;; Thus are 9 possibilities to take into account for two intervals x and y
